# Database
DATABASE_URL="postgresql://devteamos:devteamos123@localhost:5433/devteamos?schema=public"

# JWT
JWT_SECRET="replace-with-secure-secret-key"
JWT_REFRESH_SECRET="replace-with-secure-refresh-key"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"

# Google OAuth (configure later)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_CALLBACK_URL="http://localhost:3000/api/auth/google/callback"

# Email (configure later)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM="DevTeamOS <noreply@devteamos.com>"

# App
PORT=3000
FRONTEND_URL="http://localhost:5173"
UPLOAD_DIR="./uploads"
